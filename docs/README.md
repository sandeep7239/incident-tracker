# Screenshots and images

Place your screenshots here for the main README and for company submission.

## Suggested files

| File | Description |
|------|-------------|
| `swagger-ui.png` | Swagger UI at http://localhost:8080/swagger-ui.html |
| `database.png` | PostgreSQL (e.g. pgAdmin) showing `incidents` table |
| `login-screen.png` | Frontend login page |
| `incident-list.png` | Incident list (table + filters) |
| `create-incident.png` | Create incident form |
| `incident-detail.png` | Incident detail with status update |

After adding images, the root **README.md** and the **backend/README.md** and **frontend/README.md** reference them with paths like `docs/swagger-ui.png`. You can use the same names above or update the image paths in those READMEs.
